<h1>New membership</h1>

<%= form_for(@membership) do |f| %>
    Club: <%= f.select :beer_club_id, options_from_collection_for_select(@beer_clubs, :id, :to_s) %>
    <%= f.submit class: 'btn btn-primary' %>
<% end %>

<%= link_to 'Back', memberships_path %>
