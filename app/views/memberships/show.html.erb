<p>
  <strong>User:</strong>
  <%= @membership.user_id %>
</p>

<p>
  <strong>Beer club:</strong>
  <%= @membership.beer_club_id %>
</p>

<%= link_to 'Edit', edit_membership_path(@membership), class: 'btn btn-primary' %> |
<%= link_to 'Destroy', @membership, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
<br/>
<%= link_to 'Back', memberships_path %>
